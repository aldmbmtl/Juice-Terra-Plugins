{{- if eq .Values.credentials.existingSecretName "" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "juice-agent.fullname" . }}
data:
  {{ .Values.credentials.tokenKey }}: {{ .Values.token | b64enc }}
  {{ .Values.credentials.poolKey }}: {{ .Values.pool | b64enc }}
{{- end }}